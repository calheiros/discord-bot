const config = require('./default.js');

const commands = {
	help: 'help',
	private: 'private',
	hello: 'hello',
	getName: 'get-name',
	tellJoke: 'tell-a-joke',
	sad: 'sad',
	lastMsgs: 'last-messages',
};

module.exports = {
	config,
	commands
}

